{% extends 'adminBase.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Dodaj nowy artykuł{% endblock %}

{% block body %}
    <div id="wrapper">
        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Dodaj nowy artykuł</h1>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            Dodaj nowy artykuł
                        </div>
                        <div class="panel-body">
                            <div class="row">
                                <div class="col-lg-12">
                                    {{ form(form) }}

                                    {#
                                    {{ form_start(form) }}
                                    <form role="form">
                                        <div class="form-group">
                                            {{ form_row(form.name, {'attr':{'placeholder': 'Project name...', 'class': 'form-control'}}) }}
                                        </div>
                                        <div class="form-group">
                                            {{ form_row(form.description, {'attr':{'placeholder': 'Project description...', 'class': 'form-control'}}) }}
                                        </div>
                                        <div class="form-group">
                                            {{ form_row(form.demo, {'attr':{'placeholder': 'Project demo path...', 'class': 'form-control'}}) }}
                                        </div>
                                        <div class="form-group">
                                            {{ form_row(form.image, {'attr':{'placeholder': 'Project image path...', 'class': 'form-control'}}) }}
                                        </div>
                                        <button type="submit" class="btn btn-success">Submit</button>
                                        <button type="reset" class="btn btn-danger">Reset Fields</button>
                                        {{ form_rest(form) }}
                                    </form>
                                    {{ form_end(form) }}
                                    #}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}